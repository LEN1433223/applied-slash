# 性能测试手册 —— SlashBlade存储元件

三种手段各管一段,建议按 1 → 2 → 3 的顺序做:先用自检拿基线数字,再用 GameTest 确认语义没被改坏,最后进游戏量真实体感。

| 手段 | 命令 | 测什么 | 需要客户端 |
|---|---|---|---|
| ① 服务端自检 | `gradlew runServer -PselfTest` | 插入吞吐、**每 tick 重建成本曲线**、分片落盘、元件物品体积、拒收语义 | 否,自动关服 |
| ② GameTest | `gradlew runGameTestServer` | 6 项语义(拒收/优先存储/规范化/上限与重载/载荷缺失/工厂) | 否,自动退出 |
| ③ 真机压测 | 游戏内 `/appliedslash testcell N` | 驱动器、终端、存储总线的实际开销与客户端帧率 | 是 |

---

## ① 服务端自检(先跑,拿基线)

```
gradlew runServer -PselfTest
```

日志里所有 `[SELFTEST]` 行就是指标。**判据与实测参考**(窄载荷测试刀,真实整合包的刀更重,故实测值是下界):

| 指标 | 合格线 | 实测参考 |
|---|---|---|
| 非拔刀剑插入 | = 0 | 0 |
| 拔刀剑插入 / 同刀重复 | 1 /(类型 1,总数 4) | 一致 |
| 运行时组件规范化 | 插入后类型数仍 = 1 | 1 |
| 批量插入成功数 | = 配置上限 | 5000 |
| 超限插入 | = 0(且类型数不变) | 0 |
| 插入平均耗时 | < 50 µs/次 | **5–7 µs** |
| `getAvailableStacks` 最优 | 随上限对应曲线 | 1k→0.09 ms;5k→0.75–1.4;10k→1.75;20k→3.9 |
| 元件物品序列化体积 | < 2048 B **且不随存量增长** | **1089 B** |
| 分片落盘(单块最大) | ≈ 合计的 1/16 | 5k 把:6–8 ms / 54–61 ms |

**测量前先清场**:开发世界每跑一次自检就留下一个 5000 把的刀库,分片落盘成本按"该分片内所有刀库的数据量"计,反复跑会让数字从 ~6 ms 涨到 ~14 ms。

```powershell
Remove-Item run\world\data\applied_slash_blade_vault_*.dat
Remove-Item -Recurse -Force run\world   # 或整个测试世界重来
```

---

## ② GameTest(语义回归)

```
gradlew runGameTestServer
```

期望输出 `All 6 required tests passed :)`。任何一项 FAIL 都说明改动破坏了语义,不要继续做性能测试。

---

## ③ 真机压测(元件的实际影响)

### 准备

一局带 AE2 的存档:ME 网络 + ME 驱动器 + ME 终端 + **一块普通磁盘作对照组**。然后用 op 权限生成测试盘:

```
/appliedslash testcell 5000            # 给自己;控制台执行要加玩家名
```

聊天栏会回报 `已生成测试元件:类型 5000 个 / 共 5000 把,耗时 xx ms`。盘插进驱动器即可用。

### 1) 基线:空载

盘先不插(或从驱动器取出),记录 60 秒:

```
/spark profiler start --timeout 60
```

记下 MSPT / TPS 与 `MinecraftServer.tickServer` 的占比。

### 2) 满载但无人观看(关键对照)

插入测试盘,**终端关闭、不放存储监视器/等级发射器**,再测 60 秒。

> 期望:**几乎与空载无异**。AE2 的 `StorageService.onServerEndTick` 在
> `interestManager.isEmpty()` 时只置一个脏标记、不做重建,所以"没人看"时不该有常驻开销。
> 如果这一步就明显掉 TPS,说明问题在我们的插入路径或落盘策略,而不是规模。

### 3) 有人观看(真实常驻成本)

打开 ME 终端(或放一个存储监视器),保持 60 秒:

```
/spark profiler start --timeout 60
```

关注 `appeng.me.service.StorageService.onServerEndTick` 的耗时。

> 这是**唯一**会产生常驻成本的情形:AE2 每 tick 全量重建全网库存缓存,成本 ∝ 网络里的键总数(实测 5k 键 ≈ 0.75–1.4 ms/tick)。
> 把 `run/config/applied_slash-common.toml` 的 `maxTypes` 依次改成 2000 / 5000 / 10000 / 20000 各测一次,就能复现这条曲线,再决定你的服务器该用哪个值。

### 4) 自动化高频进出(吞吐与尖峰)

用**存储总线 + 箱子**做循环:导入总线持续把箱子里的刀送进网络,导出总线再吐回去。测:

```
/spark profiler --only-ticks-over 50 --timeout 120
```

> 期望:没有 >50 ms 的单 tick 尖峰。我们的插入/取出是 O(1) 内存操作 + 标脏,不做序列化;
> 落盘推迟到世界保存周期,并且**按刀哈希分 16 片**,单次只重写脏桶。
> 同时观察 `run/world/data/applied_slash_blade_vault_*.dat` 的体积增长 —— 应该是"每片各自增长",而不是某一个文件吞掉全部。

### 5) 存档尖峰

```
/save-all
```

量主线程阻塞时间。判据:单块分片耗时 ≈ 全量的 1/16(实测 5k 把:单块 6–8 ms,全量 54–61 ms)。若单块远大于这个比例,说明桶分布不均(某些刀哈希撞在一起),需要调 `hash → 桶` 的映射。

### 6) 客户端(终端同步)

- 打开 ME 终端浏览 5000 把刀:记录打开瞬间的帧率、内存增量,以及关掉终端后帧率是否立刻恢复;
- AE2 的终端同步是分包渐进式的、每 tick 有批上限,所以观察的是"打开后几秒内"的曲线,不是一次卡死;
- 记录 `logs/latest.log` 里与 `ae2` 同步相关的行;必要时加 `-Dmixin.debug.verbose` 观察;
- **注意**:同步量 ∝ 类型数 × 每键载荷。`/appliedslash testcell` 造的刀带中等偏小的刀身数据,真实整合包里更重的刀会按比例放大客户端开销。

---

## 读结果:阈值速查

| 指标 | 合格线 |
|---|---|
| 插入/取出吞吐 | < 50 µs/次(实测 5–7 µs) |
| 无人观看时的常驻成本 | ≈ 0(不得出现每 tick 固定开销) |
| 有人观看时的每 tick 成本 | 按规模取舍:2k→0.25 ms / 5k→0.75–1.4 / 10k→1.8 / 20k→3.9–5.2 |
| 单 tick 尖峰(自动化高频进出) | 无 >50 ms |
| 存档期单块分片 | ≈ 全量的 1/16 |
| 元件物品体积 | < 2 KB 且恒定 |

## 会污染测量的因素(先排除再下结论)

1. 开发世界里**历史刀库**会让分片落盘偏慢 → 按 §① 清场;
2. 首次插入会**一次性创建 16 个分片文件**,别把它算进稳态成本;
3. 只有"有人观看"才会触发每 tick 重建 —— 别把 §2 和 §3 的结果混为一谈;
4. 客户端帧率受渲染距离/光影影响,与 MSPT 是两回事,分开记;
5. `/appliedslash testcell` 生成的是**合成刀**,载荷小于真实刀;要在真实数据下测,就用整合包里真实的刀导入。
